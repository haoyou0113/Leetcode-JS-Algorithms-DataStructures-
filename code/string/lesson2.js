export default str => {
  let arr = [];
  //   any types of input, only return the first sub string that meet the Requirement
  //   create data structure stuck saving data
  let count = 0;
  // basic for loop control process
  for (let i = 0; i < str1.length - 1; i++) {
    let sub = match(str.slice(i));
    if (sub) {
    }
  }
};
